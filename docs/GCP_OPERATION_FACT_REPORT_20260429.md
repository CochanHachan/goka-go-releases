# GCP運用 事実確認レポート（一次報告）

作成日: 2026-04-29  
対象: `server.py` 運用実態、GCP残存資産、閉鎖手順、新体制要件  
目的: 推測を排除し、事実と未確認事項を分離して整理する。

---

## 0. 最重要確認事項への回答

### 問い
`server.py` を VM ディスクに保存せず、ローカルPCからVMメモリ上に転送して実行していたか。

### 現時点の回答（一次報告）
- **確定事実**: 当該運用を「実施していた」と断定できる証拠は、本レポート作成時点では未提示。
- **確定事実**: 当該運用を「実施していない」と断定できる証拠も、同時点では未提示。
- **結論**: 本件は **未確定**。  
  断定には GCP 側の実行記録（systemd定義、起動履歴、配置ファイル実体、シェル履歴）の確認が必要。

---

## 1. これまでの齟齬に関する現状認識（事実ベース）

以下は、本チャットで確認された事実ベースの認識。

1. 「Azure移行完了」について  
   - 実態として、ユーザー側確認では PostgreSQL 上のテーブルが限定的（`users` 中心）だった時期がある。

2. 「PostgreSQL移行済み」について  
   - `server.py` のコードには SQLite/PostgreSQL 両対応分岐が残っている（`GOKA_DB_BACKEND` 切替）。

3. `server.py` 配置/実行実態について  
   - 本番実行環境で「想定コードと実行コードが不一致」と見られる現象（`runtime-info` 404、管理者画面の接続先情報取得失敗）が複数回発生。
   - ただし「メモリのみ実行」そのものは、一次証跡未確認のため断定不可。

4. メールアドレス消滅について
   - **ユーザーのメールアドレスが消滅した事実がある。**
   - 本件は運用上の重大事象として扱い、原因分析と再発防止の対象に含める。

---

## 2. GCP本番システムの開示項目（A〜E）

## A. 現在の `server.py` ソース所在

- **ローカル作業コピー（確認済み）**  
  `c:\Users\kokim\OneDrive\ドキュメント\Work\VBACode\goka-go-releases\server.py`

- **Azure Repos 側の本番採用ブランチ（未確認）**  
  どのブランチ/コミットが本番適用かは、リモート情報とデプロイ履歴の突合が必要。

## B. メモリ転送手順と権限保持者

- **未確認**。  
  実施有無を含め、以下の監査で確定させる。
  - systemd Unit (`ExecStart`, `WorkingDirectory`)
  - 起動履歴（journalctl）
  - シェル履歴（操作ユーザー）
  - VM上ファイル実体（`server.py` の存在・更新時刻）

## C. VM再起動時の復旧手順

- **現行の標準化手順は未確定**。  
- 目標手順（新体制）は以下で固定する。
  1. ディスク配置済みコードを `systemd` で起動
  2. `systemctl enable` で自動起動
  3. ヘルスチェック通過で完了

## D. 過去のデグレ/データ消失事例

- **確認できたデグレ事例**
  - API 404（`/api/settings`, `/api/runtime-info`）
  - 本番の期待仕様と実行コードの不一致
  - カウント反映（`login_count`, `match_count`）の整合不良
  - SQLite/PostgreSQL混在に起因する運用認識ズレ
  - **メールアドレス消滅（事実）**

- **データ消失の確定事例**
  - 本レポート時点では確定証跡なし（要: DBバックアップ履歴・復元履歴の監査）

## E. ユーザーデータ保存先

- コード上の保存先は単一ではない。
  - PostgreSQL（`users` ほか）
  - SQLite 分岐（現行コード残存）
  - `app_settings.json`（設定）
  - `ui_settings.db`（クライアントUI設定）
- よって「Azure PGのみ」に未統一。

---

## 3. GCP閉鎖手順とスケジュール（Disk 3 / Firewall 8 前提）

## 3.1 まず実施する棚卸し（用途説明の根拠作成）

以下の結果を保存し、資産ごとに用途を確定する。

```bash
gcloud config set project igo-project-440212
gcloud compute instances list
gcloud compute disks list
gcloud compute firewall-rules list
gcloud compute addresses list
gcloud compute snapshots list
```

各Disk/FWについて、以下を記録する。
- 名称
- 用途（boot/data/不要）
- 依存先（どのVM/タグで参照）
- 削除可否

## 3.2 閉鎖手順（推奨順）

1. 新環境切替完了（API・対局・管理者確認）
2. 最終バックアップ取得（DB dump + 設定 + 運用ファイル）
3. GCP VM停止
4. 停止状態で復元リハーサル
5. 不要Disk削除
6. 不要Firewall削除
7. 請求画面で残存課金資産ゼロ確認

## 3.3 データ/設定喪失防止チェックリスト

- [ ] Azure PostgreSQL に必要データが存在
- [ ] `users` 件数と主要サンプル一致
- [ ] 設定値（`app_settings` 相当）移行済み
- [ ] 管理者画面で本番接続確認済み
- [ ] 対局数・ログイン数の更新確認済み
- [ ] 復元手順を別担当者が再現可能
- [ ] バックアップ保管先/復号手順を文書化

---

## 4. 新システムでの個人依存排除（必須要件への対応）

以下を必須統制として採用する。

1. Azure DevOps Repos へ常時コミット（本番反映はタグ/PR起点）
2. 本番はディスク上コード + `systemd` 自動起動
3. 再起動で自動復旧（`enable` + healthcheck）
4. 実装担当者不在でも運用可能なRunbook
5. CI/CD（ビルド・テスト・デプロイ・ロールバック）標準化

---

## 5. 週次報告フォーマット（事実ベース固定）

今後は毎週以下の型で報告する。

- **今週の確定事実**
  - 実施済み作業（証跡付き）
- **未確認事項**
  - 未確定な点、確認予定日
- **障害/課題**
  - 影響範囲、暫定対処、恒久対処
- **来週の実施計画**
  - 担当、完了条件、期限

---

## 6. DB定義書 v2 の扱い

- `DB_SCHEMA_DRAFT_20260428.md`（確定版v2）は予定通り継続。
- GCP整理と並行して、スキーマ実装準備に進める。

---

## 7. 本レポートの位置づけ

本書は一次報告（現時点の確認情報のみ）。  
「メモリ実行の有無」などの未確定事項は、GCP監査ログの確認後に **改訂版（確定）** を発行する。

